# WHO Global Health Intelligence Platform - reproducible dependencies
# Python: 3.9+
#
# Only packages imported directly by the application, ETL pipeline, dashboard,
# notebook generator, or automated tests are listed here. sqlite3 is provided by
# the Python standard library; SQLAlchemy, pycountry, pydantic, and pandera are
# not used by the current codebase.

# Core data processing
numpy==1.26.4
pandas==2.2.3

# WHO GHO API access
requests==2.32.3
urllib3==2.2.3

# Interactive dashboard and visualization
plotly==5.24.1
streamlit==1.40.2

# Required by plotly.express trendline="ols" in the association dashboard view
statsmodels==0.14.4

# Notebook generation/validation
nbformat==5.10.4

# Test runner
pytest==8.3.4

# Optional notebook execution outside pytest:
# Install these only if you want to run notebooks with Jupyter/nbconvert.
# jupyter==1.1.1
# nbclient==0.10.1
# ipykernel==6.29.5
