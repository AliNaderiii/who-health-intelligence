# WHO Global Health Intelligence Platform - Runtime dependencies
#
# Target runtime: Python 3.12 (see runtime.txt / .python-version)
# Deployed on Streamlit Community Cloud:
#   https://docs.streamlit.io/streamlit-community-cloud
#
# Only packages that are imported by the deployed dashboard at runtime belong
# here. Testing and tooling dependencies live in requirements-dev.txt.
#
# Every pin below ships prebuilt cp312 wheels, so the Streamlit Cloud
# dependency step never falls back to compiling from source.

# Core data processing
numpy==1.26.4
pandas==2.2.3

# WHO GHO API access - production client with retry, backoff and timeout
requests==2.32.3
urllib3==2.2.3

# Country metadata - ISO 3166 reference source
pycountry==24.6.1

# Interactive dashboard and visualisation
plotly==5.24.1
streamlit==1.40.2

# Required by plotly.express trendline="ols" in the association view
statsmodels==0.14.4
